export enum GameStatus {
    Started = 'STARTED',
    Win = 'WIN',
    Lose = 'LOSE',
    Waiting = 'WAITING',
    Unknown = 'UNKNOWN'
}

export enum Environment {
    Development = 'development',
    Production = 'production'
}